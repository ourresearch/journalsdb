parameters:
  - name: issn
    in: path
    type: string
    required: true
    default: all
definitions:
  issn:
    type: string
    properties:
      palette_name:
        type: array
responses:
  200:
    description: Pricing and Information about a scientific journal
    schema:
    examples: 